type: web  # Type of Render service for the Flask client
name: client-service
env: python
buildCommand: "pip install -r requirements.txt"
startCommand: "python app.py"
serviceDetails:
  - type: tcp
    port: 5002  # TCP port
  - type: udp
    port: 5001  # UDP port
